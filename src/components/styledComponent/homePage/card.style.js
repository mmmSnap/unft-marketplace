import Typography from "@mui/material";
import {
    Box,
    Tooltip,
    Menu,
    MenuItem,
    Divider,
    ListItemIcon,
} from "@mui/material";
import { styled } from "@mui/system";
const StyledMainBox = styled(Box)({
    flexGrow: 1,
    textAlign: "center",
    // backgroundColor:"#fff"
});

const HomePageCard = () => {
    return (
        <Box>
            <h2>Hello worldknsdkjnkd</h2>
            {/* <Typography variant="h1" component="h2">
                {'test'}
            </Typography>; */}
        </Box>
    )
}

export default HomePageCard
